<%= form_for [@manager, @farm], remote: true do |f| %>
  <div class="form-group">
    <%= f.collection_select :farmer_id, @farmers, :id, :fl_name, include_blank: true  %>
  </div>
  <div class="form-group">
    <%= f.submit "select" %>
  </div>
<% end %>
