<%= form_for @farm do |f| %>
  <div class="form-group">
    <%= f.collection_select :farmer_id, @farmers, :id,:name, include_blank: true  %>
  </div>
  <div class="form-group">
    <% f.submit "select" %>
  </div>
<% end %>
